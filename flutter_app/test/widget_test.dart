import 'package:flutter_test/flutter_test.dart';

import 'package:safenest_flutter/main.dart';

void main() {
  test('SafeNestApp can be constructed', () {
    expect(const SafeNestApp(), isNotNull);
  });
}
